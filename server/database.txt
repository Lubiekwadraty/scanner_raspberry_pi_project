CREATE DATABASE kPoint; //example name

USE kPoint;

CREATE TABLE workers(
	id INT(11) NOT NULL AUTO_INCREMENT PRIMARY KEY,
    first_name VARCHAR(40) NOT NULL,
	surname VARCHAR(35) NOT NULL,
	time_started time,
    work_time time
);

CREATE TABLE barcodes(
	id INT(11) NOT NULL AUTO_INCREMENT PRIMARY KEY,
    code BIGINT UNIQUE,
    employee_id INT(11),
	FOREIGN KEY (employee_id) REFERENCES workers(id) 
);

CREATE TABLE rfids(
	id INT(11) NOT NULL AUTO_INCREMENT PRIMARY KEY,
    code BIGINT UNIQUE,
    employee_id INT(11),
	FOREIGN KEY (employee_id) REFERENCES workers(id) 
);

//values that i used for test
INSERT INTO workers(id, first_name, surname) VALUES(1, "Stanisław", "Błąd");

INSERT INTO barcodes(id, code, employee_id) VALUES(1, 143000100019406, 1); 

INSERT INTO barcodes(id, code, employee_id) VALUES(2, 7630049203488, 1); 


//query for rfids and barcodes
SELECT workers.id, workers.first_name FROM workers LEFT JOIN barcodes ON workers.id = barcodes.employee_id LEFT JOIN rfids ON workers.id = rfids.employee_id WHERE barcodes.code=143000100019406 OR rfids.code = 143000100019406; 

//query only for barcodes
SELECT workers.id, workers.first_name FROM workers JOIN barcodes ON workers.id = barcodes.employee_id WHERE code=${code};

ALTER TABLE workers DROP COLUMN time_started; 

ALTER TABLE workers DROP COLUMN work_time; 

CREATE TABLE event_types(
	id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    code VARCHAR(20) NOT NULL,
    name VARCHAR(50)
);

CREATE TABLE event_log(
	id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    time_stamp TIMESTAMP DEFAULT NOW(),
    worker_id INT,
    type_id INT,
    FOREIGN KEY (worker_id) REFERENCES workers(id),
    FOREIGN KEY (type_id) REFERENCES event_types(id)
);

INSERT INTO event_types(code, name) VALUES("start", "Start Work");

INSERT INTO event_types(code, name) VALUES("end", "End Work");

INSERT INTO event_types(code, name) VALUES("start_break", "Start Break");

INSERT INTO event_types(code, name) VALUES("end_break", "End Break");